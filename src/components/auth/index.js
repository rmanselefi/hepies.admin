import React from 'react';
import {style} from './index.scss';

const Auth = ({}) => {

    return (
        <div className={style}>
            
        </div>
    );
};

Auth.propTypes = {

};

export default Auth;
